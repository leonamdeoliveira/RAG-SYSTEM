# RAG System — Core
zvec>=0.5.1
pydantic>=2.6
pydantic-settings>=2.2
numpy>=1.26

# RAG System — Embeddings (BGE-M3 local)
sentence-transformers>=3.0
torch>=2.1
FlagEmbedding>=1.2

# RAG System — LLM
openai>=1.30

# RAG System — ONNX backend (opcional, mais eficiente em CPU)
# Instale tambem: pip install optimum[onnxruntime]
# Para GPU AMD/Intel/NVIDIA no Windows: pip install onnxruntime-directml
onnxruntime>=1.17
tokenizers>=0.19

# RAG System — Optional
tiktoken>=0.6
langdetect>=1.0

# OCR Pipeline — Core
PyMuPDF>=1.23.0
Pillow>=10.0.0
requests>=2.31.0

# OCR Pipeline — Formatos nativos (DOCX, PPTX, XLSX, EPUB, HTML)
python-docx>=1.0.0
python-pptx>=0.6.21
openpyxl>=3.1.0
ebooklib>=0.18
beautifulsoup4>=4.12.0
html2text>=2020.1.16

# OCR Pipeline — Tesseract (opcional para OCR clássico)
pytesseract>=0.3.10

# Testing
pytest>=7.4
pytest-cov>=4.1
